import { LuxuryLoader } from "@/components/layout/LuxuryLoader";

export default function MarketingLoading() {
  return (
    <LuxuryLoader
      variant="inline"
      className="min-h-[60vh] border-t border-hairline/40"
    />
  );
}
