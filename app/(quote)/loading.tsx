import { LuxuryLoader } from "@/components/layout/LuxuryLoader";

export default function QuoteLoading() {
  return (
    <LuxuryLoader
      variant="inline"
      className="min-h-[70vh] bg-bone"
    />
  );
}
